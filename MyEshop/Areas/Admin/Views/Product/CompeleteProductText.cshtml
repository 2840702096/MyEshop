@using MyEshop.Core.Convertors
@model Comments_Questions_Answer
@inject IWeblogServices WeblogServices;

<div class="page-body">
          <!-- Container-fluid starts-->
            <div class="container-fluid">
                <div class="page-header">
                    <div class="row">
                        <div class="col-lg-6">
                            <div class="page-header-left">
                                @if(Model.IsItCommentOrQuestionOrAnswer == 1)
                        {
                                <h3>
                                    متن نظر (@Model.Title)
                                </h3>
                        }
                        else
                        {
                                if(Model.IsItCommentOrQuestionOrAnswer == 2)
                                {
                                    <h3>
                                    متن پرسش
                                    </h3>
                                }
                                else
                                {
                                    <h3>
                                    متن پاسخ
                                    </h3>
                                }
                        }
                        
                            </div>
                        </div>
                        <div class="col-lg-6">
                            <ol class="breadcrumb pull-right">
                                <li class="breadcrumb-item"><a href="/"><svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="feather feather-home"><path d="M3 9l9-7 9 7v11a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2z"></path><polyline points="9 22 9 12 15 12 15 22"></polyline></svg></a></li>
                                <li class="breadcrumb-item">محصولات</li>
                                 @if(Model.IsItCommentOrQuestionOrAnswer == 1)
                        {
                                    <li class="breadcrumb-item active">
                                    متن نظر (@Model.Title)
                                    </li>
                        }
                        else
                        {
                                if(Model.IsItCommentOrQuestionOrAnswer == 2)
                                {
                                    <li class="breadcrumb-item active">
                                    متن پرسش
                                    </li>
                                }
                                else
                                {
                                    <li class="breadcrumb-item active">
                                    متن پاسخ
                                    </li>
                                }
                        }
                            </ol>
                        </div>
                    </div>
                </div>
            </div>
            <!-- Container-fluid Ends-->
     <div class="container-fluid">
                <div class="card">
                    <div class="card-header">
                        @if(Model.IsItCommentOrQuestionOrAnswer == 1)
                        {
                                <h5>
                                    متن نظر (@Model.Title)
                                </h5>
                        }
                        else
                        {
                                if(Model.IsItCommentOrQuestionOrAnswer == 2)
                                {
                                    <h5>
                                    متن پرسش
                                    </h5>
                                }
                                else
                                {
                                    <h5>
                                    متن پاسخ
                                    </h5>
                                }
                        }
                    </div>
                    <div class="card-body">
                        <div class="row">
    <div class="col-lg-12">
        <div class="panel panel-default">
            <!-- /.panel-heading -->
            <div class="panel-body">
                <div class="table-responsive">
                    <div id="dataTables-example_wrapper" class="dataTables_wrapper form-inline" role="grid">
                        <table class="table table-striped table-bordered table-hover dataTable no-footer" id="dataTables-example" aria-describedby="dataTables-example_info">
                            <thead>
                                <tr>
                                    @if(Model.IsItCommentOrQuestionOrAnswer == 1)
                        {
                                <th class="text-center" style="vertical-align:middle">متن نظر</th>
                        }
                        else
                        {
                                if(Model.IsItCommentOrQuestionOrAnswer == 2)
                                {
                                    <th class="text-center" style="vertical-align:middle">متن پرسش</th>
                                }
                                else
                                {
                                    <th class="text-center" style="vertical-align:middle">متن پاسخ</th>
                                }
                        }
                                </tr>
                            </thead>

                            <tbody>
                                                        <tr>
                                                            @if(Model.IsItCommentOrQuestionOrAnswer == 1)
                                                    {
                                                        <td>@Model.CommentBody</td>
                                                    }
                                                    else
                                                    {
                                                        if(Model.IsItCommentOrQuestionOrAnswer == 2)
                                                        {
                                                            <td>@Model.QuestionBody</td>
                                                        }
                                                        else
                                                        {
                                                            <td>@Model.AnswerBody</td>
                                                        }
                                                    }
                                                        </tr>
                                            
                            </tbody>
                        </table>
                    </div>
                </div>
                <div>
                    @if(Model.IsItCommentOrQuestionOrAnswer == 1)
                        {
                                <a class="btn btn-light" href="/Admin/Comments">
                        بازگشت</a>
                        }
                        else
                        {
                                if(Model.IsItCommentOrQuestionOrAnswer == 2)
                                {
                                    <a class="btn btn-light" href="/Admin/Questions">
                        بازگشت</a>
                                }
                                else
                                {
                                    <a class="btn btn-light" href="/Admin/Answers">
                        بازگشت</a>
                                }
                        }
                    
                </div>
                <!-- /.table-responsive -->
            </div>
            <!-- /.panel-body -->
        </div>
        <!-- /.panel -->
    </div>
    <!-- /.col-lg-12 -->
</div>
                    </div>
                </div>
            </div>
            <!-- Container-fluid Ends-->
        </div>