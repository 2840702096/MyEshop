@model MyEshop.Core.DTOs.EditSliderViewModel

@{
    ViewData["Title"] = "ویرایش اسلایدر";
}

<div class="page-body">
    <!-- Container-fluid starts-->
    <div class="container-fluid">
        <div class="page-header">
            <div class="row">
                <div class="col-lg-6">
                    <div class="page-header-left">
                        <h3>
                            ویرایش اسلایدر
                        </h3>
                    </div>
                </div>
                <div class="col-lg-6">
                    <ol class="breadcrumb pull-right">
                        <li class="breadcrumb-item"><a href="/Admin/Home"><i data-feather="home"></i></a></li>
                        <li class="breadcrumb-item">اسلایدر ها</li>
                        <li class="breadcrumb-item active">
                            ویرایش اسلایدر
                        </li>
                    </ol>
                </div>
            </div>
        </div>
    </div>
    <!-- Container-fluid Ends-->
    <!-- Container-fluid starts-->
    <div class="container-fluid">
        <div class="row product-adding">
            <div class="col-xl-12">
                <div class="card">
                    <div class="card-header">
                        <h5>اطلاعات اسلایدر</h5>
                    </div>
                    <div class="card-body">
                        <div class="digital-add needs-validation">
                            <form asp-action="EditProductCategorySlider" method="post" enctype="multipart/form-data">
                                <input type="hidden" value="@Model.CurrentImage" name="currentImage"/>
                                <div class="form-group">
                                    <label class="col-form-label pt-0"><span>*</span>عنوان اسلایدر</label>
                                    <input asp-for="Title" class="form-control" type="text">
                                    <span class="text-danger" asp-validation-for="Title"></span>
                                </div>
                                <div class="form-group">
                                    <label class="col-form-label pt-0"><span>*</span>آدرس اسلایدر</label>
                                    <input asp-for="Link" class="form-control" type="text">
                                    <span class="text-danger" asp-validation-for="Link"></span>
                                </div>
                                <div class="form-group">
                                    <label class="col-form-label pt-0"><span>*</span>کلمات کلیدی</label>
                                    <input placeholder="با علامت , جدا کنید" asp-for="Tags" class="form-control" type="text">
                                    <span class="text-danger" asp-validation-for="Tags"></span>
                                </div>
                                <div class="form-group">
                                    <label class="col-form-label pt-0">
                                        <span>*</span>گروه
                                        اسلایدر
                                    </label>
                                    <select asp-for="CategoryId" id="Group"
                                            class="form-control" asp-items="@ViewBag.CategoryOfSlider">
                                    </select>
                                </div>
                                <div class="form-group">
                                    <label class="col-form-label pt-0"><span>*</span>تصویر</label>
                                    <button type="button"
                                            style="display:block;width:120px; height:30px;border: 1px solid;border-radius: 3px;"
                                            onclick="document.getElementById('getFile').click()">
                                        بارگذاری تصویر
                                    </button>
                                    <input type="file" class="image" id="getFile" style="display:none"
                                           asp-for="ImageName">
                                    <span class="text-danger" asp-validation-for="ImageName"></span>
                                </div>
                                <div>
                                    <img id="Product" class="imgPreview" src="/Admin/images/ProductCategorySliderImages/@Model.CurrentImage"
                                         style="max-width:150px;max-height:150px;margin-bottom:10px" />
                                </div>
                                <div class="form-group mb-0">
                                    <div class="product-buttons text-center">
                                        <button type="submit" class="btn btn-primary">ذخیره</button>
                                        <a href="/Admin/ProductSliderList" class="btn btn-light">بازگشت</a>
                                    </div>
                                </div>
                            </form>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <!-- Container-fluid Ends-->

</div>

@section Scripts
    {
    <script src="https://cdn.ckeditor.com/4.9.2/standard/ckeditor.js"></script>

    <script>
        function readURL(input) {

            if (input.files && input.files[0]) {
                var reader = new FileReader();

                reader.onload = function(e) {
                    $('#Product').attr('src', e.target.result);
                }

                reader.readAsDataURL(input.files[0]);
            }
        }

        $("#getFile").change(function() {
            readURL(this);
        });
    </script>
}